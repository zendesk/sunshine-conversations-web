dependencies:
    override:
        - npm install:
            timeout: 600e_modules/nightwatch-commands/selenium/drivers/chromedriver:
            timeout: 10

test:
    override:
        - grunt test:
            timeout: 600